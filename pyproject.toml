[project]
name = "hanabi"
version = "0.1.0"

[tool.setuptools]
package-dir = {"" = "web/backend"}

[tool.coverage.run]
omit = [
    "web/backend/server/domain/exceptionMapper.py",
    "web/backend/server/domain/gameInterface.py",
]

[tool.setuptools.packages.find]
where = ["web/backend"]